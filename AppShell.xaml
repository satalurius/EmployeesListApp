<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="EmployeesListApp.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:EmployeesListApp"
    xmlns:pages="clr-namespace:EmployeesListApp.Views"
    Shell.FlyoutBehavior="Disabled">

    <!--<ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />
    -->

    <!--<FlyoutItem FlyoutDisplayOptions="AsSingleItem" Route="employees_page">
        <Tab Title="Test">
        <ShellContent
            Title="Главная"
            Icon="dotnet_bot.svg"
            ContentTemplate="{DataTemplate pages:EmployeesPage}"
            Route="employees_page"
            />
        </Tab>

    </FlyoutItem>-->

    <TabBar>
            <ShellContent
                Title="Главная"
                Icon="dotnet_bot.svg"
                ContentTemplate="{DataTemplate pages:EmployeesPage}"
                Route="employees_page"
            />
            <ShellContent
                Title="О проекте"
                Icon="dotnet_bot.svg"
                ContentTemplate="{DataTemplate pages:EmployeesPage}"
                Route="."
            />
    </TabBar>

</Shell>
